name: Apply Osoli audit V4

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: write

jobs:
  staging-guard:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch staging is intentionally paused."
