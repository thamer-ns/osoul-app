name: Apply exhaustive audit V4 patch

on:
  pull_request:
    types: [synchronize]
    branches: [main]

permissions:
  contents: write

jobs:
  staging-guard:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Audit patch staging is intentionally paused."
