# تقرير التدقيق الهندسي الشامل — أصولي V4

**المستودع:** `thamer-ns/osoul-app`
**النسخة التي بدأ منها التدقيق:** `748f06dd62b715a3d718099dc93c2208e5718ab9`
**تاريخ التدقيق:** 2026-07-27
**حالة التقرير:** دفعة إصلاح أولى قابلة للمراجعة، وليست شهادة جاهزية إنتاج نهائية.

## 1. نطاق العمل

شمل التدقيق جرد المستودع كاملًا، وتحليل ملفات Python نحويًا، ومراجعة المسارات عالية الخطورة يدويًا، وتشغيل اختبارات ديناميكية مركزة على:

- تأكيد اكتمال الشمعة وعدم استخدام الشمعة الحية في القرار.
- المؤشرات الفنية المتقدمة وتوقيت الدايفرجنس وحساب RLS.
- سلامة JSON المخزن في PostgreSQL ومنع `NaN` و`Infinity`.
- جلب الأسعار، الإغلاق السابق، مصدر البيانات، وحالة السعر القديم.
- قراءة قاعدة البيانات، تجمعات الاتصالات، وعزل مسار SQLAlchemy عن المسار الخام.
- تقييم مخاطر المحفظة وترجيح المضاربة بالقيمة السوقية.
- كلمات المرور والإعدادات الحساسة وملفات التشخيص.

بلغت النسخة المفحوصة قرابة **139 ملفًا متتبعًا**، منها **120 ملف Python**. تم التركيز العميق على مسارات القرار المالي والفني والأمني بدل اعتبار نجاح الفحص الشكلي دليلًا كافيًا على صحة التطبيق.

## 2. الحكم التنفيذي

المشروع قوي كنواة منصة عربية لإدارة المحافظ والتحليل، لكن نتائج المراجعة أثبتت أن تقارير CI السابقة التي تعرض «0 تحذيرات» لم تكن كافية لاكتشاف أخطاء منطقية مؤثرة. أهم مثال كان اعتماد آخر صف سعري باعتباره شمعة مغلقة دون تحقق زمني مركزي، رغم أن الواجهة تصف الإشارة بأنها مؤكدة بالإغلاق.

### التقييم الحالي

| الجانب | قبل دفعة V4 | بعد دفعة V4 الحالية | الملاحظة |
|---|---:|---:|---|
| سلامة القرار الفني | 5.5/10 | 7.5/10 | أضيفت بوابة مركزية للشموع المكتملة وعولج توقيت الدايفرجنس وRLS. |
| صحة بيانات السوق | 6/10 | 7.5/10 | لم يعد غياب الإغلاق السابق يتحول إلى تغير زائف 0%. |
| المحاسبة والتحليلات | 7/10 | 7.5/10 | تحسن تقييم السعر القديم والمخاطر، ولا تزال العمولات وتوقيت التدفقات بحاجة تطوير. |
| قاعدة البيانات والأداء | 6.5/10 | 7.5/10 | أزيل احتجاز اتصال psycopg2 أثناء قراءة SQLAlchemy وأصلح تهيئة المحرك. |
| الأمان | 6.5/10 | 7/10 | أزيل fallback كلمة المرور الضعيف وملف تشخيص خطر؛ ما زال الحد من الدخول وإبطال الجلسات بحاجة طبقة خادمية. |
| قابلية الصيانة | 5.5/10 | 6.5/10 | أضيفت عقود واختبارات، لكن بعض الوحدات شديدة التضخم وطبقات الـMonkey Patch لا تزال دينًا معماريًا. |
| الجاهزية للإنتاج العام | 5/10 | 6.5/10 | أفضل بوضوح، لكنها لا تزال تحتاج البنود المفتوحة في قسم المخاطر المتبقية. |

## 3. الأخطاء المؤكدة التي عولجت

### P0 — قرار فني قد يعتمد شمعة غير مكتملة

**الخلل:** كان محرك التقرير والحزمة الفنية يستهلكان آخر صف من المزود مباشرة. وجود عبارة «تأكيد بالإغلاق» داخل وصف الإشارة لا يضمن أن الصف نفسه انتهى زمنيًا.

**الأثر:** اختراق أو كسر أو ضرب وقف قد يظهر أثناء تكوّن الشمعة ثم يختفي، ويعطي مظهر إعادة رسم أو إشارة غير منضبطة.

**الإصلاح:**

- إضافة `candle_confirmation.py` بوصفه المصدر المركزي للتحقق من اكتمال الشموع.
- مراعاة توقيت السوق السعودي، الفواصل اللحظية، اليومي، الأسبوعي المنتهي الخميس، والشهري.
- تطبيق الفلتر قبل التقرير الفني، المؤشرات المتقدمة، التحليل الكلاسيكي، المختبر، وتقييم النتائج المتعلمة.
- حفظ عدد الصفوف المستبعدة ووقت آخر شمعة مغلقة ضمن البيانات الوصفية.

### P0 — JSON غير صالح بسبب NaN وInfinity

**الخلل:** يمكن للمؤشرات إنتاج قيم غير منتهية أو مفقودة، بينما `json.dumps` الافتراضي في Python يسمح بكتابة `NaN` و`Infinity`، وهي ليست JSON قياسيًا وقد يرفضها PostgreSQL `JSONB`.

**الأثر:** فشل صامت في حفظ الحزمة أو النتيجة، ثم إعادة الحساب في كل مرة، أو اختلاف بين الواجهة والتخزين.

**الإصلاح:**

- إنشاء `ai_engine_core/json_utils.py`.
- تحويل القيم غير المحدودة إلى `null` بصورة متكررة وآمنة.
- استخدام `allow_nan=False` لجميع الحمولات الحساسة في قاعدة البيانات.
- إضافة اختبارات تضمن أن النص النهائي JSON قياسي وقابل للتحميل.

### P1 — RLS ينتج توقعات غير منطقية على الفواصل الدقيقة

**الخلل:** كان الميل يُسنّن باستخدام افتراضات جلسة أمريكية وثوابت قد تؤدي إلى نسب فلكية أو `Infinity` على الدقيقة.

**الإصلاح:**

- إعادة توصيفه كـ **Adaptive Trend** بدل ادعاء توقع سنوي.
- حساب ميل لوغاريتمي لكل شمعة، ثم إسقاط محدود لعدد شموع يتكيف مع الفاصل.
- منع القيم غير المنتهية وإظهار أفق التقدير بوضوح.

### P1 — الدايفرجنس يظهر في شمعة الارتكاز قبل إمكان معرفته

**الخلل:** إشارة الدايفرجنس كانت تحمل زمن Pivot نفسه، رغم أن تأكيد الارتكاز يحتاج شموعًا لاحقة على اليمين.

**الأثر:** الاختبار الخلفي أو الرسم قد يوحي بأن الإشارة كانت معروفة مبكرًا.

**الإصلاح:** فصل `pivot_index` عن `confirmed_index` وإسناد الإشارة إلى شمعة التأكيد الفعلية.

### P1 — Value Area غير متصلة حول POC

**الخلل:** اختيار أعلى الصناديق حجمًا عالميًا قد ينتج منطقة قيمة متقطعة ذات فجوات.

**الإصلاح:** التوسع بصورة متجاورة من POC إلى الأعلى أو الأسفل حتى بلوغ نسبة الحجم المطلوبة. لا يزال المؤشر تقريبًا مبنيًا على توزيع حجم الشمعة، وليس Volume-at-Price حقيقيًا من الصفقات المنفذة.

### P1 — تغير يومي زائف 0%

**الخلل:** بعض المصادر الاحتياطية كانت تستبدل `prev_close` المفقود بالسعر الحالي.

**الأثر:** ظهور تغير 0% وكأنه معلومة مؤكدة، مع أنه في الحقيقة غير متاح.

**الإصلاح:** الإغلاق السابق المفقود يبقى غير متاح، و`change_pct=None` مع `change_available=False`.

### P1 — مضاعفة اتصالات قاعدة البيانات في القراءة

**الخلل:** مسار القراءة كان يحصل على اتصال من pool الخام ثم ينفذ الاستعلام فعليًا بمحرك SQLAlchemy، فيحتجز اتصالين وقد يرفع زمن الانتظار تحت الضغط.

**الإصلاح:**

- مسار SQLAlchemy لا يحصل على اتصال خام مسبقًا.
- استخدام الاتصال الخام فقط كمسار احتياطي حقيقي.
- إضافة Sentinel يمنع إعادة محاولة إنشاء المحرك عند فشل تهيئته بصورة متكررة.
- ضبط حجم pool بصورة محافظة.
- اعتبار الجدول الفارغ نتيجة ناجحة بدل إعادة الاستعلام عبر مسار آخر.

### P1 — السعر المخزن القديم يظهر كأنه حديث

**الخلل:** عند فشل السعر الحي، كان السعر المخزن يُستخدم دون وسم `stale` في بعض الحالات.

**الإصلاح:** السعر المخزن الاحتياطي يبقى قابلًا للاستخدام المحاسبي، لكنه موسوم كمعلومة قديمة ويخفض جودة البيانات.

### P1 — نسبة المضاربة غير موزونة ماليًا

**الخلل:** كانت نسبة الأسهم المضاربية تعتمد عدد المراكز؛ مركز صغير يعادل مركزًا ضخمًا.

**الإصلاح:** الترجيح أصبح بالقيمة السوقية، مع رسالة تعرض النسبة الفعلية بدل حد نصي مختلف عن حد الكود.

### P1 — fallback ضعيف لكلمات المرور

**الخلل:** الوحدة الأساسية احتوت fallback مباشرًا إلى SHA-256 عند غياب bcrypt، رغم وجود Patch لاحق وقت التشغيل.

**الأثر:** السلوك الآمن كان يعتمد على ترتيب الاستيراد وتشغيل `app.py`.

**الإصلاح:** الدوال الأساسية نفسها أصبحت تفشل مغلقة عند غياب bcrypt، ولا تنشئ تجزئة ضعيفة جديدة.

### P1 — ملف تشخيص خطر داخل المستودع

تم حذف `.streamlit/test_connection.py` لأنه يعرض معلومات اتصال وأعداد سجلات وعينات ورسائل خام، ولا ينبغي وجوده في نسخة إنتاج.

### P2 — إعدادات وأثر CI

- توحيد أسماء متغيرات البيئة مع الأسماء التي يقرأها البرنامج فعليًا.
- إزالة رفع نسخة المصدر المؤقتة من Workflow.
- إزالة تقرير تدقيق مولد من المستودع.
- توسيع `.gitignore` للملفات المؤقتة والنتائج المحلية.
- إضافة اختبارات مركزة إلى CI بدل الاكتفاء بفحوص Syntax ضيقة.

## 4. الاختبارات المنفذة محليًا

نجحت الدفعة الحالية في:

- `58 passed` عبر pytest في بيئة الاختبار المحلية.
- `compileall` لجميع ملفات Python دون أخطاء Syntax.
- اختبارات اكتمال الشمعة اليومية واللحظية والأسبوعية والشهرية.
- اختبار JSON الصارم ومنع `NaN/Infinity`.
- اختبار أن قراءة SQLAlchemy لا تحجز اتصال pool خامًا.
- اختبار عقد السعر عند غياب الإغلاق السابق.
- اختبار ترجيح مخاطر المحفظة بالقيمة السوقية.
- اختبارات المؤشرات الفنية العامة والحالات الحدية المضافة.

التحقق النهائي المعتمد يجب أن يكون من GitHub Actions على SHA المرفوع، لأن البيئة المحلية استخدمت بدائل اختبارية للمكتبات غير المثبتة في الحاوية.

## 5. المخاطر المفتوحة التي لم تُخفَ أو يُدّعَ حلها

### P0/P1 قبل نشر جماهيري

1. **Rate limiting لتسجيل الدخول محصور في Session المتصفح:** يمكن تجاوزه بجلسة أو متصفح جديد. المطلوب سجل محاولات خادمي أو Redis/DB keyed بالهوية وIP بعد مراعاة الخصوصية.
2. **إبطال الجلسة:** التوكن موقّع ومنتهي، لكن لا توجد طبقة مركزية قوية لإبطال جلسة بعينها أو جميع جلسات المستخدم بعد تغيير كلمة المرور.
3. **عزل PostgreSQL:** الفلترة التطبيقية جيدة نسبيًا، لكن الحماية النهائية ينبغي أن تشمل Foreign Keys وقيود ملكية وRow-Level Security حيث يناسب.
4. **العمولات والتنفيذ:** محرك القيمة يحتاج سياسة موحدة مؤكدة للعمولات والضرائب والانزلاق والبيع الجزئي في جميع المسارات القديمة والجديدة.
5. **TWR وتوقيت التدفقات:** التجميع اليومي يحتاج سياسة دقيقة لاعتبار التدفق بداية اليوم أو نهايته، خصوصًا عند وجود صفقة وتدفق في اليوم نفسه.
6. **تدوير الأسرار القديمة:** فحص النسخة الحالية لا يجعل أي مفتاح سبق نشره في تاريخ Git آمنًا؛ التدوير والتنظيف إجراء تشغيلي خارجي.

### P2 — جودة وهندسة

1. `generate_ai_report` ووحدات التحليل الكلاسيكي ما زالت شديدة التضخم والتعقيد وتحتاج تفكيكًا إلى مراحل قابلة للاختبار.
2. توجد طبقات Monkey Patch كثيرة تجعل صحة السلوك تعتمد على ترتيب الاستيراد؛ يلزم نقل الإصلاحات تدريجيًا إلى الوحدات الأصلية وحذف الرقع.
3. بعض عمليات `st.cache_data.clear()` عامة وقد تمس كاش مستخدمين آخرين؛ الأفضل مفاتيح إصدار خاصة بالمستخدم والمحفظة.
4. مدد كاش التاريخ اللحظي طويلة نسبيًا لبعض حالات السوق المفتوح، وينبغي ضبطها حسب الفاصل وحالة الجلسة.
5. الاختبارات الحالية لا تكفي لإثبات الربحية أو جودة التنبؤ؛ يلزم Walk-forward، عينة خارجية، تكاليف تنفيذ، ومنع Look-ahead على مستوى كامل خط البيانات.
6. RTL والأداء البصري يحتاجان اختبار متصفح فعلي على سطح المكتب والجوال بعد نشر Preview؛ نجاح Python لا يثبت سلامة DOM/CSS.

## 6. توصيات التحليل الفني والبيانات

- أبقِ **direction_score** منفصلًا عن **confidence** وعن **data_quality**.
- لا تُسمِّ RLS توقعًا سعريًا موثوقًا قبل قياس خطأ خارج العينة؛ «اتجاه متكيف» أدق.
- خزّن مع كل خطة: آخر شمعة مغلقة، وقت الإنشاء، نسخة المحرك، مصدر البيانات، حالة السعر، وأسباب الإبطال.
- قيّم الإشارات باستخدام Precision/Recall وExpectancy وصافي R بعد التكلفة، لا إجمالي الربح وحده.
- قارن النتائج حسب السوق والفاصل والحالة الاتجاهية، ولا تخلط الدقيقة باليومي في نموذج جودة واحد.
- ميّز Volume Profile التقريبي المبني على OHLCV عن بيانات الصفقات الفعلية عند السعر.

## 7. قرار الدمج

الدفعة الحالية تستحق المراجعة على **PR Draft** وتشغيل CI، لكنها لا ينبغي أن تُدمج أو تُنشر تلقائيًا قبل:

1. نجاح جميع بوابات GitHub على SHA نهائي ثابت.
2. مراجعة Diff النهائي والتأكد من عدم تغير عقود واجهات قديمة دون توافق.
3. اختبار Preview ببيانات سوق فعلية وحسابين ومحفظتين.
4. اختبار شمعة حية ثم إغلاقها على فاصل لحظي للتأكد من انتقال الإشارة مرة واحدة فقط.
5. أخذ نسخة احتياطية من قاعدة البيانات قبل أي Migration إضافي.

## 8. الخلاصة

دفعة V4 لم تضف مؤشرات لمجرد زيادة المزايا؛ بل أصلحت نقاطًا تؤثر مباشرة في صدق القرار: هل الشمعة مغلقة فعلًا، هل الإشارة كانت قابلة للمعرفة في وقتها، هل الرقم صالح للتخزين، هل السعر حديث، وهل الاتصال بقاعدة البيانات يستخدم الموارد بكفاءة.

بعد هذه الإصلاحات أصبحت البنية الفنية أكثر صدقًا وقابلية للاختبار، لكن المشروع ما زال يحتاج جولة ثانية مركزة على المصادقة الخادمية، قيود قاعدة البيانات، المحاسبة التنفيذية، وتفكيك الوحدات الضخمة قبل اعتباره منصة مالية عامة مكتملة.
