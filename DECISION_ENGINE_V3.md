# محرك القرار المركزي V3

## الهدف

توحيد نتيجة التحليل في قرار واحد قابل للتفسير بدل ترك التنفيذ موزعًا بين السكور والثقة وبوابات المخاطر والواجهة. المحرك لا يجلب بيانات جديدة ولا يكرر المؤشرات؛ بل يستهلك التقرير الموجود مرة واحدة ثم يبني نتيجة نهائية آمنة ومتسقة لكل صفحات التطبيق.

## الأفكار المدمجة من البوت والمؤشر

- فصل **اتجاه الإشارة** عن **ثقة الأدلة**.
- اعتماد الاختراق والكسر وضرب الوقف بعد **إغلاق الشمعة** فقط.
- دورة واضحة للخطة: مراقبة، قابلة للتنفيذ، محظورة، لا توجد فرصة، ثم أهداف T1/T2/T3 أو إبطال أو انتهاء.
- معرف حتمي للخطة يمنع اعتبار التقرير نفسه فرصة جديدة عند إعادة تشغيل واجهة Streamlit.
- تصنيف الفرص إلى اختراق/كسر قوي، استرداد/رفض سيولة، تحول زخم، فرصة هيكلية أو توافق فائق.
- تخفيض الثقة عند تعارض المحرك الأساسي مع المؤشرات المتقدمة، أو تعارض الفواصل، أو ضعف السيولة، أو فشل جودة البيانات، أو معاكسة اتجاه السوق.
- خطة مخاطر اتجاهية متكيفة مع ATR: منطقة دخول، وقف، ثلاثة أهداف، نسبة عائد إلى مخاطرة، إبطال وانتهاء حسب الفاصل.
- منع واجهة الإشارات من إطلاق تنبيه دخول لخطة HEADS_UP أو BLOCKED أو NO_SETUP.
- عدم عرض traceback أو تفاصيل الخطأ الداخلية للمستخدم؛ يظهر رقم تتبع آمن وتبقى التفاصيل في سجل الخادم.

## الدمج الجزئي المقصود

- أضيف عقد دورة حياة ومُعرّف خطة، لكن لم تُنقل قاعدة بيانات Telegram أو آلية Webhook؛ التطبيق ليس ناقل تنبيهات Telegram.
- لم تُترجم ملفات Pine الخاصة بالمؤشر لأنها غير موجودة في مستودع البوت، ولأن ترجمة منطق غير متاح ستكون ادعاءً غير قابل للتحقق.
- لم يُفعّل تعديل الأوزان تلقائيًا من النتائج التاريخية الجديدة؛ التعلم يبقى خلف بواباته الحالية حتى تتوفر عينة مستقلة وناضجة.
- لم تُضف ادعاءات ربحية أو Profit Factor أو صافي R؛ هذه المقاييس تحتاج سياسة تنفيذ وخروج فعلية وبيانات مستقبلية سليمة.
- لم يُنقل كل تصنيف خاص بأسواق العملات والعقود؛ التطبيق الحالي يركز على محفظة المستخدم والسوق المدعوم وبياناته المتاحة.

## عقد النتيجة

يضيف التقرير النهائي الحقول التالية:

- `direction` و`direction_score`
- `opportunity_type` و`opportunity_label`
- `lifecycle_status`
- `risk_plan` ويشمل `plan_id`, `entry_low`, `entry_high`, `stop`, `target1..3`, `expiry_bars`
- `decision_engine` ويشمل البوابات والتعارضات والعقوبات والانتقالات المستقبلية
- `engine_meta.decision_engine_version`

## قواعد الأمان الفني

- لا تتحول الخطة إلى `ACTIONABLE` عند فشل بوابة المخاطر أو السيولة.
- التعارض الاتجاهي يخفض الثقة ويمنع التصنيف الفائق.
- الخطة المحايدة لا تحصل على دخول أو وقف أو أهداف مصطنعة.
- مستويات الشراء والبيع تُبنى بترتيب اتجاهي صحيح.
- تفاصيل الخطأ الداخلية لا تعاد إلى الواجهة.
