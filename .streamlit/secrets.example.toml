# ضع أسرارك هنا محلياً فقط ولا ترفعها إلى Git
# انسخ الملف إلى: .streamlit/secrets.toml

DATABASE_URL = "postgresql://USER:PASSWORD@HOST:PORT/DBNAME"
AUTH_SECRET = "ضع_سراً_قوياً_هنا"
TWELVEDATA_API_KEY = "YOUR_API_KEY"

# خيارات سياسات كلمة المرور
MIN_PASSWORD_LEN = "8"
ALLOW_LEGACY_PIN = "1"
REQUIRE_LETTER_DIGIT = "0"
