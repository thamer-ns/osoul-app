# ترقية أصولي إلى البنية الآمنة v2

## قبل الدمج أو النشر

1. غيّر كلمة مرور قاعدة البيانات وأصدر `DATABASE_URL` جديدًا.
2. ألغِ مفتاح Twelve Data القديم وأصدر مفتاحًا جديدًا.
3. أنشئ `AUTH_SECRET` عشوائيًا جديدًا بطول 32 حرفًا على الأقل.
4. ضع القيم الجديدة في Streamlit Secrets أو متغيرات بيئة منصة النشر، ولا تضفها إلى Git.
5. اترك `ALLOW_REGISTRATION=0` ما لم تكن تريد السماح بإنشاء حسابات جديدة.

> حذف ملف الأسرار من الفرع لا يلغي القيم التي ظهرت سابقًا في تاريخ المستودع العام. بعد تدويرها، نظّف تاريخ Git باستخدام `git filter-repo` أو BFG ثم نفّذ force-push في نافذة صيانة.

## ترحيل البيانات

عند أول تشغيل بعد الترقية، تضيف `tenant_db.ensure_tenant_schema()` حقل `user_id` إلى جداول المحفظة والحركات النقدية وقائمة المتابعة والأطروحات، كما تضيف رسوم الدخول والخروج وملاحظات الصفقة وسجل تدقيق.

- إذا كانت قاعدة البيانات تحتوي مستخدمًا واحدًا فقط، تُسند السجلات القديمة إليه تلقائيًا.
- إذا كانت تحتوي أكثر من مستخدم، تبقى السجلات القديمة غير المملوكة مخفية حمايةً للخصوصية إلى أن يحدد المسؤول مالك كل سجل.

مثال إسناد يدوي بعد أخذ نسخة احتياطية:

```sql
UPDATE trades SET user_id = 'USERNAME' WHERE user_id IS NULL;
UPDATE deposits SET user_id = 'USERNAME' WHERE user_id IS NULL;
UPDATE withdrawals SET user_id = 'USERNAME' WHERE user_id IS NULL;
UPDATE returnsgrants SET user_id = 'USERNAME' WHERE user_id IS NULL;
```

## فحص ما بعد النشر

- سجّل الدخول بحسابين مختلفين وتأكد أن كل حساب يرى بياناته فقط.
- أضف إيداعًا وصفقة، ثم عدّلها وأغلق الصفقة وتأكد من الحسابات.
- راجع NAV وXIRR وتأكد أن زيادة الإيداعات لا تظهر كربح استثماري.
- تحقق من أن مصدر السعر ووقت آخر شمعة يظهران بصورة صحيحة.
- اختبر إشارة اختراق على فاصل لحظي ويومي وتأكد أنها لا تعتمد إلا بعد إغلاق الشمعة.
- راجع نتائج GitHub Actions قبل الدمج.

## الرجوع الآمن

لا تحذف الأعمدة القديمة أثناء هذه الترقية. عند الحاجة إلى الرجوع، أعد توجيه الراوتر إلى الصفحات القديمة، مع العلم أن `views/portfolio.py` أصبح غلاف توافق للنسخة v2 لمنع خطأ Syntax السابق.
