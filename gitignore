# .gitignore
*.db            # لا ترفع قاعدة بياناتك الحقيقية والمالية!
__pycache__/    # ملفات بايثون المؤقتة
*.pyc
.streamlit/     # إعدادات الثيم (اختياري، لكن يفضل عدم رفعه إذا كان يحتوي أسراراً)
backups/        # لا ترفع النسخ الاحتياطية
.DS_Store       # لمستخدمي الماك
.env            # إذا كنت ستستخدم مفاتيح API مستقبلاً
